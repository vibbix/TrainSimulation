This is Tin's Passenger class and PassengerTest class. JavaDoc are written up, JUnit test runs fine. Might need to implement more JUnit Test methods.

Currently it tests toString() and embarkTrainTest().
